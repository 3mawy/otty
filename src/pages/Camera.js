import React, {useState} from 'react';
import ReactCamera from "../components/Camera/ReactCamera";
import {BiCamera, BiUpload} from "react-icons/bi";

const Camera = () => {
    //ReactCamera

    return (
        <div>
            <ReactCamera />
        </div>
    );
};

export default Camera;